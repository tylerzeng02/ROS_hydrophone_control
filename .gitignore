# CMake build output -- but track the real collected/measured data files
# that live in build/ (that's just where every program in this project
# reads/writes them by default), not the actual build artifacts.
build/*
!build/*.csv
!build/*.md
cmake-build-*/

# Archive/trash/reference folders -- historical or reference material kept
# on disk for whoever's working locally, deliberately not tracked in git
# (previously build/archive/ was the one exception that was
# tracked. Now every archive/trash folder is treated the same way).
build/archive/
calibration/archive/
ros/archive/
tests/archive/
src/archive/
trash/
understanding/
notes/
pid_tuning/

# Compiled files
*.o
*.a
*.so
*.out

# Python
__pycache__/
*.pyc

# ROS 2 build output (colcon workspace lives at ros/, not ros2_ws/)
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/
ros/build/
ros/install/
ros/log/

# pymoveit2 is a manually-cloned dependency (see ros/src/fus_targeting_gui/
# README.md's Setup section), not vendored in this repo. Ignored so a
# plain `git add .` after cloning it can't accidentally recreate the
# dangling gitlink-with-no-.gitmodules-entry this project once had here.
ros/src/pymoveit2/

# Colcon build output accidentally left at repo root or inside
# calibration/current/ from running colcon from the wrong directory --
# these are pure regenerable artifacts, never meant to be tracked (one
# instance found still had a hardcoded path from a
# different machine baked into it).
/install/
/log/
calibration/current/build/
calibration/current/install/
calibration/current/log/

# Auto-generated per-session logs from fus_targeting_gui (one new file per
# GUI launch, written by main_window.py's _start_csv_log()). These are a
# running usage log, not curated test/calibration data, so new ones should
# not be pushed by default.
ros/fus_targeting_session_*.csv

# Editor and OS files
.vscode/
.idea/
.DS_Store
*.swp

# Local project history/notes, not meant to be published with the repo.
CLAUDE.md