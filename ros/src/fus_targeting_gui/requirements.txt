# Third-party PyPI dependencies for fus_targeting_gui -- colcon/rosdep don't
# manage these (no ROS package for most of them), so install with pip into
# whatever Python environment `ros2 run` uses, before building/running this
# package:
#
#   pip install -r requirements.txt
#
# rclpy, moveit_msgs, geometry_msgs, sensor_msgs, tf_transformations come
# from the ROS 2 install itself (see package.xml's exec_depend list) --
# not listed here.

PySide6>=6.5
pyvista>=0.43
pyvistaqt>=0.11
pymoveit2 @ git+https://github.com/AndrejOrsula/pymoveit2.git
PyYAML>=6.0
